server:
  port: 8088
spring:
  profiles:
    active: ${default-profile}
  application:
    name: dm-ribbon
  cloud:
    nacos:
      config:
        server-addr: logicer.top:8848
        namespace: ${config.namespace}
        shared-configs[0]:
          data-id: common.yaml
          refresh: true
        file-extension: yaml
      discovery:
        namespace: ${config.namespace}
        server-addr: logicer.top:8848
        ip: {ip: 127.0.0.1}