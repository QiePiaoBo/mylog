server:
  port: 8889
spring:
  profiles:
    active: dev
  application:
    name: config-server
  cloud:
    nacos:
      config:
        server-addr: http://127.0.0.1:8848
        file-extension: yaml
        enabled: true