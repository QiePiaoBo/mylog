server:
  port: 9011
spring:
  application:
    name: ds-blog
  profiles:
    active: ${default-profile}
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://logicer.top:3306/ds_blog?characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false&serverTimezone=UTC
    username: root
    password: 1997o4
    type: com.alibaba.druid.pool.DruidDataSource
  main:
    allow-bean-definition-overriding: true
  redis:
    host: logicer.top
    port: 6379
    database: 0
    password: 1997o4
    jedis:
      pool:
        max-idle: 10
        max-active: 200
        max-wait: -1
        min-idle: 0
  cloud:
    nacos:
      config:
        server-addr: logicer.top:8848
        namespace: ${config.namespace}
        file-extension: yaml
        refresh-enabled: true
        shared-configs[0]:
          data-id: common.yaml
          refresh: true
      discovery:
        namespace: ${config.namespace}
        server-addr: logicer.top:8848
        ip: {ip: 127.0.0.1}
  session:
    store-type: redis
mybatis-plus:
  mapper-locations: classpath:mybatis/mapper/*Mapper.xml